# Database (PostgreSQL connection string)
DATABASE_URL=postgresql://user:password@host:port/database

# Application
PORT=7002
NODE_ENV=development

# JWT Secret (used for signing auth cookies)
JWT_SECRET=your_jwt_secret_key_here
